# Server-side proxy target. Keep this URL private so browser requests remain on
# the frontend origin and authentication cookies remain first-party.
BACKEND_API_URL=http://localhost:4000

# Backward-compatible build-time fallback for existing deployments. New
# deployments should use BACKEND_API_URL instead.
NEXT_PUBLIC_API_URL=http://localhost:4000
